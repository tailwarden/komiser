import { TemplateProps } from './Template';

const base: TemplateProps = {
  sampleTextProp: 'Hello, you can change me :)'
};

const mockTemplateProps = {
  base
};

export default mockTemplateProps;
